apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.service.name }}-data
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "qtest-elasticsearch.labels" . | nindent 4 }}
    elasticsearch.role: data
spec:
  serviceName: {{ .Values.service.name }}-data-headless
  replicas: {{ .Values.data.replicas }}
  podManagementPolicy: Parallel
  selector:
    matchLabels:
      {{- include "qtest-elasticsearch.selectorLabels" . | nindent 6 }}
      elasticsearch.role: data
  template:
    metadata:
      labels:
        {{- include "qtest-elasticsearch.selectorLabels" . | nindent 8 }}
        elasticsearch.role: data
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        traffic.sidecar.istio.io/excludeOutboundPorts: "9300"
        traffic.sidecar.istio.io/excludeInboundPorts: "9300"
    spec:
      securityContext:
        fsGroup: {{ .Values.data.podSecurityContext.fsGroup }}
        runAsUser: {{ .Values.data.podSecurityContext.runAsUser }}
        runAsNonRoot: {{ .Values.data.podSecurityContext.runAsNonRoot }}
      terminationGracePeriodSeconds: 120
      {{- if .Values.initSysctl.enabled }}
      initContainers:
        - name: sysctl
          image: busybox:1.36
          command: ["sysctl", "-w", "vm.max_map_count=262144"]
          securityContext:
            privileged: true
            runAsUser: 0
      {{- end }}
      containers:
        - name: elasticsearch
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.httpPort }}
              protocol: TCP
            - name: transport
              containerPort: {{ .Values.service.transportPort }}
              protocol: TCP
          env:
            - name: node.name
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: node.roles
              value: "data,ingest"
            - name: ES_JAVA_OPTS
              value: "-Xms{{ .Values.data.heapSize }} -Xmx{{ .Values.data.heapSize }}"
            {{- if not .Values.security.enabled }}
            - name: xpack.security.enabled
              value: "false"
            {{- end }}
          volumeMounts:
            - name: config
              mountPath: /usr/share/elasticsearch/config/elasticsearch.yml
              subPath: elasticsearch.yml
              readOnly: true
            - name: data
              mountPath: /usr/share/elasticsearch/data
          resources:
            {{- toYaml .Values.data.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /_cluster/health?local=true
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /_cluster/health?local=true
              port: http
            initialDelaySeconds: 90
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 5
      volumes:
        - name: config
          configMap:
            name: {{ .Values.service.name }}-config
        {{- if not .Values.data.persistence.enabled }}
        - name: data
          emptyDir: {}
        {{- end }}
      {{- with .Values.data.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.data.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.data.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- if .Values.data.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ {{ .Values.data.persistence.accessMode | quote }} ]
        {{- if .Values.data.persistence.storageClass }}
        storageClassName: {{ .Values.data.persistence.storageClass | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.data.persistence.size }}
  {{- end }}
