{{- if .Values.externalSecrets.enabled }}
# ═══════════════════════════════════════════════════════════════════
# ExternalSecret: qtest-manager-secret
# ═══════════════════════════════════════════════════════════════════
# Assembles 16 GSM secrets (one value each) into one K8s Secret with
# 16 keys. Consumed by qtest-mgr and all subcharts.
# Note: LaunchDarkly uses 1 GSM secret → 2 K8s keys (dot + hyphen variants).
#
# DB password keys hold the dummy value "unused" — the Cloud SQL Auth
# Proxy sidecar intercepts JDBC connections on 127.0.0.1:5432 and
# substitutes a live IAM token. The keys must exist so the app's
# secretKeyRef lookups succeed at pod startup.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: qtest-manager-secret-es
  namespace: {{ .Values.namespace.name }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  secretStoreRef:
    name: gcp-secret-store
    kind: SecretStore
  target:
    name: qtest-manager-secret
    creationPolicy: Owner
  data:
    # ── Database Passwords (dummy "unused" — proxy handles real auth) ──
    - secretKey: client.jdbc.postgres.password
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.dbPassword | default "qtest-db-password" }}
    - secretKey: client.jdbc.postgres.readonly.password
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.dbReadonlyPassword | default "qtest-db-readonly-password" }}

    # ── OAuth App Secrets (5 — internal microservice tokens) ──────────
    # Jenkins, Bamboo, Tosca, and Web Explorer are not used.
    - secretKey: oauth.app.sp.secret
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.oauthSpSecret | default "qtest-oauth-sp-secret" }}
    - secretKey: oauth.app.sessions.secret
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.oauthSessionsSecret | default "qtest-oauth-sessions-secret" }}
    - secretKey: oauth.app.explorer.secret
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.oauthExplorerSecret | default "qtest-oauth-explorer-secret" }}
    - secretKey: oauth.app.qmap.secret
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.oauthQmapSecret | default "qtest-oauth-qmap-secret" }}
    - secretKey: oauth.app.pulse.secret
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.oauthPulseSecret | default "qtest-oauth-pulse-secret" }}

    # ── Jira OAuth2 Keys (REQUIRED — startup-mandatory internal keys) ─
    # These are NOT Jira integration credentials. They initialise qTest's
    # internal OAuth2 token encryption subsystem at startup.
    # ⚠️  qTest will NOT start if either key is empty.
    - secretKey: jira.oauth2.password
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.jiraOauth2Password | default "qtest-jira-oauth2-password" }}
    - secretKey: jira.oauth2.salt
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.jiraOauth2Salt | default "qtest-jira-oauth2-salt" }}

    # ── Session & CSRF Keys (REQUIRED) ──────────────────────────────
    - secretKey: sessions.secret.key
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.sessionsSecretKey | default "qtest-sessions-secret-key" }}
    - secretKey: csrfKey
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.csrfKey | default "qtest-csrf-key" }}
    - secretKey: csrf.secret.key
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.csrfSecretKey | default "qtest-csrf-secret-key" }}

    # ── Pulse Executor API Key (REQUIRED) ───────────────────────────
    - secretKey: qtestPulseExecutorApiKey
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.pulseExecutorApiKey | default "qtest-pulse-executor-api-key" }}

    # ── Mail Password (REQUIRED by Insights pod startup — empty is valid) ─
    - secretKey: mail.password
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.mailPassword | default "qtest-mail-password" }}

    # ── LaunchDarkly SDK Key (OPTIONAL — both key formats for subchart compat) ─
    - secretKey: launchdarkly.sdkKey
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.launchdarklySdkKey | default "qtest-launchdarkly-sdk-key" }}
    - secretKey: launchdarkly-sdkKey
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.launchdarklySdkKey | default "qtest-launchdarkly-sdk-key" }}
---
# ═══════════════════════════════════════════════════════════════════
# ExternalSecret: qtest-aes-secret-keys
# ═══════════════════════════════════════════════════════════════════
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: qtest-aes-secret-keys-es
  namespace: {{ .Values.namespace.name }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  secretStoreRef:
    name: gcp-secret-store
    kind: SecretStore
  target:
    name: qtest-aes-secret-keys
    creationPolicy: Owner
  data:
    - secretKey: AESsecretKeys
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.aesSecretKeys | default "qtest-aes-secret-keys" }}
---
# ═══════════════════════════════════════════════════════════════════
# ExternalSecret: qtest-scenario-secret
# ═══════════════════════════════════════════════════════════════════
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: qtest-scenario-secret-es
  namespace: {{ .Values.namespace.name }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  secretStoreRef:
    name: gcp-secret-store
    kind: SecretStore
  target:
    name: qtest-scenario-secret
    creationPolicy: Owner
  data:
    - secretKey: qTestScenarioRefreshTokenSecret
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.scenarioRefreshTokenSecret | default "qtest-scenario-refresh-token-secret" }}
    - secretKey: jiraCypherKey
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.jiraCypherKey | default "qtest-jira-cypher-key" }}
---
# ═══════════════════════════════════════════════════════════════════
# ExternalSecret: qtest-i-etl-secret
# ═══════════════════════════════════════════════════════════════════
# The qtest-i-etl deployment has hardcoded secretKeyRef lookups for
# these 4 keys. All DB connections go through the Cloud SQL Auth Proxy
# sidecar on 127.0.0.1:5432 — the proxy substitutes an IAM token.
# Values stored in GSM are the dummy string "unused".
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: qtest-i-etl-secret-es
  namespace: {{ .Values.namespace.name }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  secretStoreRef:
    name: gcp-secret-store
    kind: SecretStore
  target:
    name: qtest-i-etl-secret
    creationPolicy: Owner
  data:
    # ── All 4 keys are dummy "unused" — proxy handles real auth ───────
    - secretKey: qtestDbPassword
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.iEtlQtestDbPassword | default "qtest-i-etl-db-password" }}
    - secretKey: sessionsWriteDbPassword
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.iEtlSessionsWriteDbPassword | default "qtest-i-etl-sessions-write-password" }}
    - secretKey: sessionsReadDbPassword
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.iEtlSessionsReadDbPassword | default "qtest-i-etl-sessions-read-password" }}
    - secretKey: insightsDbPassword
      remoteRef:
        key: {{ .Values.externalSecrets.remoteRefs.iEtlInsightsDbPassword | default "qtest-i-etl-insights-db-password" }}
{{- end }}
